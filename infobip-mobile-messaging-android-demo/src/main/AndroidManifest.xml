<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.infobip.mobile.messaging.demo">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".InspectActivity"
            android:label="@string/title_activity_inspect">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>

        <activity
            android:name=".WebViewActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>

        <receiver android:name=".MobileMessagingBroadcastReceiver" android:exported="false">
            <intent-filter>
                <action android:name="org.infobip.mobile.messaging.REGISTRATION_ACQUIRED" />
                <action android:name="org.infobip.mobile.messaging.REGISTRATION_CREATED" />
                <action android:name="org.infobip.mobile.messaging.API_COMMUNICATION_ERROR" />
            </intent-filter>
        </receiver>

        <receiver android:name=".WebContentReceiver" android:exported="false">
            <intent-filter>
                <action android:name="org.infobip.mobile.messaging.NOTIFICATION_TAPPED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
